(executable
  (public_name wasm)
  (modules wasm)
  (libraries wasm)
  (flags (-open Wasm))
)

(rule
  (targets wasm.ml)
  (deps ../src/vendor/wasm/interpreter/main/main.ml)
  (action (copy ../src/vendor/wasm/interpreter/main/main.ml wasm.ml))
)
